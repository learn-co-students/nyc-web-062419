import React from "react"

class Scores extends React.Component {

    render() {
        return (
            <>
                {/* <img alt="" src={this.state.baconImages[Math.floor(Math.random() * this.state.baconImages.length)]} className="img" />: score
                <br />
                <img alt="" src={this.state.cakeSauceImages[Math.floor(Math.random() * this.state.cakeSauceImages.length)]} className="img" />: score */}
            </>
        )
    }
}

export default Scores