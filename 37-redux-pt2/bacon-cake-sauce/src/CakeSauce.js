import React from 'react'

const CakeSauce = () => {
    return (
        <img alt="" src={this.state.cakeSauceImages[Math.floor(Math.random() * this.state.cakeSauceImages.length)]} />: score

    )
}

export default CakeSauce